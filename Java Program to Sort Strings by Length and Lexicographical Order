import java.util.*;
class Main{
    public static void main(String args[]){
        Scanner sc=new Scanner(System.in);
        int n=sc.nextInt();
        sc.nextLine();
        String []s=new String[n];
        for(int i=0;i<n;i++){
            s[i]=sc.nextLine();
        }
        Arrays.sort(s,(a,b)->a.length()==b.length()?a.compareTo(b):a.length()-b.length());
        System.out.print(Arrays.toString(s));
    }
}
